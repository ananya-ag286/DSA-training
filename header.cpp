#include<iostream>
#include"myStack.h"
#include<math.h>
#include<vector>
#include<stdlib.h>
using namespace std;
int stacktop(Stack *fs)
{
    return fs->item[s.top];
}
int main()
{
    Stack ms;
    initialize(&ms);
    int d;
    cin>>d;
    while(d!=0)
    {
        push(&ms,d%2)
        
    }
    
}